var text = "Hello World" + "! :D";
var neverAssigned = {};
neverAssigned.name = "Samuel Ricardo";
var tobeReassigned = {};
tobeReassigned = {
  name: "ana"
};
tobeReassigned.name = 1;
tobeReassigned = 0;
tobeReassigned = {
  name: "ana"
};
let result = test.split(",").map(letter => letter.toUpperCase()).join(".");
console.log(result);
